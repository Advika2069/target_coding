def cake(n,arr):
    high=arr[0]
    for i in range(len(arr)):
        if arr[i]>high:
            high=arr[i]
    count= 0
    for i in range(n):
        if arr[i]==high:
            count+=1
    return count
n = int(input())
arr = list(map(int,input().split()))
print(cake(n,arr))
